# CLAUDE.md

## 1. Project Documents

프로젝트 요구사항과 데이터 구조는 다음 문서를 기준으로 한다.

* `docs/PRD.md` → 프로젝트 요구사항
* `docs/ERD.md` → 데이터베이스 구조

PRD와 ERD에 없는 기능을 임의로 추가하지 않는다.

---

## 2. Development Principles

* 기존 코드를 먼저 확인한 후 수정한다.
* 필요한 범위만 수정한다.
* 기존 기능을 깨뜨리지 않는다.
* 과도한 추상화와 복잡한 아키텍처를 만들지 않는다.
* 이미 사용하는 라이브러리로 해결할 수 있다면 새로운 라이브러리를 추가하지 않는다.
* 반복되는 UI와 로직은 적절히 재사용한다.
* 한 번만 사용하는 코드를 억지로 추상화하지 않는다.

---

## 3. Code Quality

* TypeScript를 사용한다.
* 명확한 이름을 사용한다.
* 컴포넌트는 하나의 명확한 역할을 갖는다.
* 중복 코드를 최소화한다.
* 사용하지 않는 코드와 의존성을 남기지 않는다.
* 에러와 민감한 정보를 사용자에게 노출하지 않는다.

---

## 4. UI / UX

* 일관된 디자인 시스템을 유지한다.
* https://ui.shadcn.com/docs/components 홈페이지의 shadcn 컴포넌트를 우선적으로 활용한다
* Mobile / Tablet / Desktop을 모두 고려한다.
* 접근성을 기본적으로 준수한다.
* 과도한 애니메이션과 불필요한 UI를 사용하지 않는다.
* 기능보다 시각적 장식을 우선하지 않는다.

---

## 5. Data & Security

* 환경변수와 Secret을 코드에 직접 작성하지 않는다.
* `.env.local` 등 환경변수 파일의 Secret을 Git에 커밋하지 않는다.
* 사용자 입력은 적절하게 검증한다.
* DB 구조를 변경할 경우 `docs/ERD.md`와 일치하도록 유지한다.

---

## 6. Before / After Coding

### Before

1. 관련 PRD 확인
2. 현재 코드 구조 확인
3. 기존 컴포넌트와 로직 확인
4. 가장 단순한 구현 방법을 선택

### After

1. TypeScript 오류 확인
2. Lint 오류 확인
3. Build 오류 확인
4. 기존 기능의 정상 동작 확인

---

## 7. Important

AI가 프로젝트의 방향을 임의로 결정하지 않는다.

요구사항이 불명확하면 추측해서 구현하지 말고 필요한 내용을 확인한다.

**Simple, consistent, maintainable.**
